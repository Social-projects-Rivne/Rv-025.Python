language: python
python:
  - "2.7"
install:
  - pip install flake8
  - pip install -r requirements.txt
before_script:
  - flake8 . --ignore=E501,W291,W293
  - psql -c 'create database restaurants_db;’ -U postgres
script:
  - python ./manage.py test
